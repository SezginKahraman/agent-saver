---
description: List saved agents
---

Call the MCP tool `list_agents` with no arguments, unless the user passes `--project` or `--global`, in which case pass the appropriate `scope` argument (`project` or `global`).

Render the returned markdown table verbatim.
